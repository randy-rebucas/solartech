listener 1883
listener 9001
protocol websockets
# Dev broker defaults: enable anonymous clients so the
# container can start without a mounted password file.
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
log_type error
log_type warning
log_type notice
log_type information
